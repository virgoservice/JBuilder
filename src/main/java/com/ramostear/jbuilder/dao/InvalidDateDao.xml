<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ramostear.jbuilder.dao.InvalidDateDao">
		
	
	<resultMap type="com.ramostear.jbuilder.entity.InvalidDate" id="resultMap">
		<id column="id" property="id"/>
		<result column="ticketId" property="ticketId"/>
		<result column="beginDate" property="beginDate"/>
		<result column="endDate" property="endDate"/>
	</resultMap>
	
	<insert id="save" parameterType="com.ramostear.jbuilder.entity.InvalidDate" useGeneratedKeys="true" keyColumn="id" keyProperty="id">
		insert into order_invaliddate
		<trim prefix="(" suffix=")" suffixOverrides=",">
			<if test="ticketId != null">ticketId,</if>
			<if test="beginDate != null">beginDate,</if>
			<if test="endDate != null">endDate</if>
		</trim>
		<trim prefix="values(" suffix=")" suffixOverrides=",">
			<if test="ticketId != null">#{ticketId},</if>
			<if test="beginDate != null">#{beginDate},</if>
			<if test="endDate != null">#{endDate}</if>
		</trim>
	</insert>	
	
	<update id="update" parameterType="com.ramostear.jbuilder.entity.InvalidDate">
		update order_invaliddate
		<set>
			<if test="ticketId != null">ticketId=#{ticketId},</if>
			<if test="beginDate != null">beginDate=#{beginDate},</if>
			<if test="endDate != null">endDate=#{endDate}</if>
		</set>
		where id = #{id}
	</update>
	
	<select id="size" resultType="Long">
		select count(*) from order_invaliddate
	</select>
	
	
	<delete id="delete" parameterType="Long">
		delete from order_invaliddate 
		where 
		id = #{id}
	</delete>

	<select id="findById" parameterType="Long" resultMap="resultMap">
		select * from order_invaliddate 
		where 
		id = #{id}
	</select>
	
	<select id="findAll"  resultMap="resultMap">
		select * from order_invaliddate
	</select>
	
	<select id="findByPage" resultMap="resultMap">
		select * from order_invaliddate jn 
		order by ${orderBy} 
		<if test="order == true">
			DESC
		</if>
		<if test="order == false">
			ASC
		</if>
		limit #{offset},#{size}
	</select>
	
</mapper>